<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".ImpostorGameApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImpostorGame">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3713745689837812~3618998223" />

        <activity android:name=".activities.PlayGameActivity" android:exported="false" />
        <activity android:name=".activities.ClassicRevealActivity" android:exported="false" />
        <activity android:name=".activities.CoverRevealActivity" android:exported="false" />
        <activity android:name=".activities.CountdownActivity" android:exported="false" />
        <activity android:name=".activities.VoteActivity" android:exported="false" />
        <activity android:name=".activities.VictoryActivity" android:exported="false" />
        <activity android:name=".activities.GuessWordActivity" android:exported="false" />

        <!--
            SplashActivity es ahora el punto de entrada de la app.
            noHistory=true evita que al pulsar Atrás desde MainActivity
            el usuario regrese a la pantalla de intro.
        -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity ya no es launcher; SplashActivity la inicia directamente -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

    </application>

</manifest>
